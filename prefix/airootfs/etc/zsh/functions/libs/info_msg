#!/usr/bin/env zsh
emulate -L zsh; setopt extendedglob warncreateglobal pipefail nounset

print -P "\n%F{#00c897}%K{#00241b} ◉ %* %D{%d-%m-%Y} ┆ ${1:-} %F{#00241b}%K{reset}%F{reset}" # Perform prompt expansion (see EXPANSION OF PROMPT SEQUENCES in zshmisc(1)).
